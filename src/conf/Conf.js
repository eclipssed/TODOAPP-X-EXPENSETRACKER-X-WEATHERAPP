const conf = {
    weatherApiKey: String(import.meta.env.VITE_WEATHER_API_KEY),
    domainKey: String(import.meta.env.VITE_DOMAIN_KEY),
    clientId: String(import.meta.env.VITE_CLIENT_ID),
  };
  
  export default conf;
  